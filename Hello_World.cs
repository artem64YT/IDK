using UnityEngine;

public class Hello_World : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //Вывод в консоль приветствие миру
        print("Hello, World");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
